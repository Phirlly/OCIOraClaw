schemaVersion: 1.1.0
version: "2026.04.05"
locale: "en"
groupings:
  - title: "OpenClaw Access"
    visible: true
    items:
      - ssh_public_key
      - oci_genai_api_key

  - title: "OpenClaw Instance"
    visible: true
    items:
      - availability_domain
      - instance_display_name
      - instance_shape
      - image_id
      - instance_ocpus
      - instance_memory_gbs

  - title: "Hidden Resource Manager Context"
    visible: false
    items:
      - compartment_ocid

variables:
  compartment_ocid:
    type: oci:identity:compartment:id
    title: "Compartment OCID"
    description: "Deployment compartment, automatically prepopulated by Resource Manager."
    visible: false
    required: true

  availability_domain:
    type: oci:identity:availabilitydomain:name
    title: "Availability Domain"
    description: "Select the availability domain for the compute instance."
    required: true
    dependsOn:
      compartmentId: ${compartment_ocid}

  ssh_public_key:
    type: oci:core:ssh:publickey
    title: "SSH Public Key"
    description: "Public SSH key to be included in the authorized_keys file on the instance."
    required: true

  oci_genai_api_key:
    type: password
    title: "OCI Generative AI API Key"
    description: "API key used at instance startup to discover working chat-completions models for OpenClaw."
    required: true

  instance_display_name:
    type: string
    title: "Instance Name"
    description: "Display name for the OpenClaw compute instance."
    default: "openclaw"
    required: true

  instance_shape:
    type: oci:core:instanceshape:name
    title: "Instance Shape"
    description: "Select the compute shape for the instance."
    required: true
    dependsOn:
      compartmentId: ${compartment_ocid}
      availabilityDomain: ${availability_domain}

  image_id:
    type: oci:core:image:id
    title: "Image"
    description: "Select an Oracle Linux 9 image compatible with the selected shape."
    required: true
    dependsOn:
      compartmentId: ${compartment_ocid}
      shape: ${instance_shape}
      operatingSystem: "Oracle Linux"
      operatingSystemVersion: "9"

  instance_ocpus:
    type: integer
    title: "OCPUs"
    description: "Number of OCPUs for the selected flexible instance shape."
    default: 2
    minimum: 1
    required: true

  instance_memory_gbs:
    type: integer
    title: "Memory (GB)"
    description: "Memory in GB for the selected flexible instance shape."
    default: 12
    minimum: 1
    required: true
